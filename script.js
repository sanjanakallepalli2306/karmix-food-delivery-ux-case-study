function showPrototype(event){
  event.preventDefault();
  const message = document.getElementById("prototypeMessage");
  message.textContent = "Replace this button's link with your Figma prototype URL before publishing to GitHub.";
}